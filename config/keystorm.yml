
defaults: &defaults
  opennebula:
    endpoint: http://localhost:2633/RPC2
    secret: oneadmin:opennebula
    api-call-timeout: 3h
  log_level: debug

production:
  <<: *defaults
  log_level: info

development:
  <<: *defaults

test:
  <<: *defaults
